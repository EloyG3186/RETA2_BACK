const chatController = require('./chatController');
const communityController = require('./communityController');
const testimonialController = require('./testimonialController');
const friendNetworkController = require('./friendNetworkController');
const enrichedCommentController = require('./enrichedCommentController');

module.exports = {
  chatController,
  communityController,
  testimonialController,
  friendNetworkController,
  enrichedCommentController
};
